#ifndef EXPER_H

enum {LOAD_VECTORS, FAULT_TOLERANCE, SLAVE_INTRO};
#define EXPERIMENT_TYPE FAULT_TOLERANCE

enum {POINT_QUERIES, RANGE_QUERIES, MIXED_QUERIES};
#define QUERY_TYPE RANGE_QUERIES

#endif
